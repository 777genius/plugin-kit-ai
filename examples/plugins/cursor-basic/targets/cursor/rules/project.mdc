---
description: "Keep repo-local Cursor automation aligned with the authored package contract"
globs:
  - "**/*"
alwaysApply: false
---

- Treat `src/plugin.yaml`, `src/mcp/servers.yaml`, and `src/targets/cursor/**` as the authored source of truth.
- Keep generated `.cursor/**` artifacts in sync through `plugin-kit-ai generate`.
- Treat root `CLAUDE.md` and `AGENTS.md` as plugin boundary docs that explain the generated/plugin-root split.
