# v1.2.2 Release Notes

## Summary

- release tag: `v1.2.2`
- candidate commit SHA: commit tagged by `v1.2.2`
- release type: stable patch
- one-sentence user summary: integration lifecycle tests now tolerate Codex unavailable status in CI while still validating the same planned actions.

## Public-Stable In This Release

- No user-facing CLI behavior changed from `v1.2.1`.
- CI assertions now validate Codex lifecycle actions independently from environment-specific target availability status.

## Why This Release Matters

GitHub runners do not have the Codex app or CLI available, so report status emoji can differ from local machines even when the integration action is correct. This patch keeps Required CI focused on the stable action text instead of the runner-specific status marker.

## What Changed For Users

- No migration required.
- `plugin-kit-ai skills install|list|update|remove` remains the latest stable external skills wrapper surface.

## External Smoke Status

- required: local targeted lifecycle test passed before patch tagging.
- release-gate: planned before patch tagging.
- release-preflight: planned through GitHub Actions for `v1.2.2`.
- release-assets: planned through GitHub Actions for `v1.2.2`.
- downstream publishes: planned through release workflow after assets publish.

## Known Limitations

- Same as `v1.2.1`: the external skills wrapper requires Node/npm and network access because it delegates to `npx -y skills@1.5.5`.

## Decision Record

- final audit outcome: patch-approved as a CI stability correction for the `v1.2.x` skills release line.
- maintainer sign-off: local release evidence collected by maintainer before tagging.
