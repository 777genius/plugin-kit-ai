# v1.2.1 Release Notes

## Summary

- release tag: `v1.2.1`
- candidate commit SHA: commit tagged by `v1.2.1`
- release type: stable patch
- one-sentence user summary: `plugin-kit-ai` now includes the generated docs for the new external skills commands and keeps integration lifecycle tests stable across CI environments.

## Public-Stable In This Release

- Generated CLI docs for `plugin-kit-ai skills install|list|update|remove`.
- CI-stable integration lifecycle assertion for Codex targets when the target app is not available on the runner.

## Why This Release Matters

`v1.2.0` published the external skills wrapper successfully, but push CI exposed missing generated website docs and an environment-sensitive test expectation. This patch keeps the released surface the same while making the release commit clean for Required and Docs checks.

## What Changed For Users

- No command behavior changed from `v1.2.0`.
- Website reference docs now include the new external skills subcommands.

## External Smoke Status

- required: local `make release-gate` passed before patch tagging.
- docs: local `website` `pnpm run docs:check` passed before patch tagging.
- release-preflight: planned through GitHub Actions for `v1.2.1`.
- release-assets: planned through GitHub Actions for `v1.2.1`.
- downstream publishes: planned through release workflow after assets publish.

## Known Limitations

- Same as `v1.2.0`: the external skills wrapper requires Node/npm and network access because it delegates to `npx -y skills@1.5.5`.

## Decision Record

- final audit outcome: patch-approved as a CI/docs correction for the `v1.2.x` skills release line.
- maintainer sign-off: local release evidence collected by maintainer before tagging.
