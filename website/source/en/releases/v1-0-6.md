---
title: "v1.0.6"
description: "User-facing release notes for the runtime-package delivery and verification hardening release."
canonicalId: "page:releases:v1-0-6"
section: "releases"
locale: "en"
generated: false
translationRequired: true
---

# v1.0.6

Release date: `2026-03-30`

## Choose In 60 Seconds

- Starting a new non-Go plugin repo: prefer Node/TypeScript with `--runtime-package`.
- Staying Python-first in a repo-local project: prefer Python with `--runtime-package`.
- Still treating vendored helper files as the default long-term path: re-check your decision now.

## Read This Release If

- you are deciding between vendored helpers and `--runtime-package`
- you are the Python or Node owner for a plugin repo
- you want the latest supported recommendation for non-Go delivery
- your team needs one note to link when discussing the current Python or Node default

## Why This Release Matters

`v1.0.6` is the release where shared runtime-package delivery became a real public path for Python and Node teams instead of a partial or caveated workflow.

## What Changed For Users

- `plugin-kit-ai init` can now scaffold Python and Node projects around `--runtime-package`
- the shared `plugin-kit-ai-runtime` path is treated as a supported delivery model for the approved Python and Node lanes
- starter templates and public examples were refreshed to use the current pinned versions and the clearer runtime-package story
- release verification and version-sync checks were tightened so the published surface stays aligned with the documented install path

## Who Should Act On This Release

- teams starting a new Python or Node plugin repo now
- teams planning to standardize how runtime helpers are delivered across repos
- teams still relying on older vendored-helper assumptions for long-term maintenance

## What This Means In Practice

- if your team wants the mainstream non-Go stable lane, Node/TypeScript plus `--runtime-package` is now a first-class recommendation
- if your team is Python-first and staying repo-local, the shared runtime package is now the preferred supported path over vendored helper drift
- `doctor` and runtime requirement messaging are stronger, so environment failures are easier to catch before blaming the authored project

## Recommended Defaults After v1.0.6

- choose Go when you want the strongest production path and the least downstream bootstrap friction
- choose Node/TypeScript with `--runtime-package` when you want the mainstream non-Go stable lane
- choose Python with `--runtime-package` when the project is repo-local and the team is intentionally Python-first

## What Did Not Change

- Go is still the strongest default when you want the most battle-tested production path.
- Runtime-package delivery is still not a claim that every language lane has identical support or behavior.
- Guides and reference pages remain the place to learn the mechanics; this note tells you which recommendation became stronger.

## Migration Callout

If you previously treated vendored runtime helper files as the default long-term model for Python or Node, re-check the current guidance. For the approved shared-package lanes, `plugin-kit-ai-runtime` is now the clearer supported path.

## If You Should Change Course Now

- Change now if you are creating a fresh Python or Node repo and want the recommended supported path.
- Re-evaluate now if you were about to standardize vendored helpers as the long-term default.
- Keep the current path temporarily if you are mid-release, but update your next planned starter or delivery decision against this note.

## What To Read Next

- Read [Choose Delivery Model](/en/guide/choose-delivery-model) if you are deciding between vendored helpers and shared runtime packages.
- Read [Bundle Handoff](/en/guide/bundle-handoff) if your Python or Node lane must travel as a portable artifact.
- Read [Install Channels](/en/reference/install-channels) if you also need the CLI installation story, not only the runtime-package delivery story.

Pair this release with [Installation](/en/guide/installation), [Choosing Runtime](/en/concepts/choosing-runtime), and [Install Channels](/en/reference/install-channels).
