# {{.ProjectName}}

Gemini extension scaffold generated by [plugin-kit-ai](https://github.com/777genius/plugin-kit-ai) `init`.

## Status

- Packaging contract: `gemini-extension.json`, shared MCP, primary root context, and Gemini-native extension assets
- Lifecycle: copy-install for releases, `gemini extensions link` for local development, restart Gemini CLI after changes
- Platform family: `extension_package`
- Launcher contract: `none`
- Runtime claim: `packaging-only target`

## First Run

```bash
plugin-kit-ai generate .
plugin-kit-ai generate --check .
plugin-kit-ai validate . --platform gemini --strict
```

## Target Files

- `src/plugin.yaml`: portable package manifest
- no `src/launcher.yaml`: Gemini packaging does not use the executable launcher contract unless you intentionally scaffold the Go runtime lane
- `src/mcp/servers.yaml`: optional portable MCP authored source; `plugin-kit-ai init --extras` pre-seeds a starter and generate projects it into inline Gemini `mcpServers`
- `src/targets/gemini/package.yaml`: compact Gemini extension metadata such as `context_file_name`, `exclude_tools`, and `plan_directory`
- `src/targets/gemini/contexts/GEMINI.md`: primary authored extension context starter
- `src/targets/gemini/commands/`: TOML slash commands
- `src/targets/gemini/hooks/`: `hooks/hooks.json`
- `src/targets/gemini/policies/`: extension policy files
- `src/targets/gemini/settings/`: install-time settings that generate into `settings[]`
- `src/targets/gemini/themes/`: extension themes that generate into `themes[]`
- `src/targets/gemini/manifest.extra.json`: forward-compatible escape hatch for future Gemini manifest fields
- root `CLAUDE.md` and `AGENTS.md`: boundary docs that tell humans and agents to edit only `src/`

Generated artifacts come from `plugin-kit-ai generate`:

- `gemini-extension.json`
- selected root context file, usually `GEMINI.md`
- native Gemini file trees copied from `targets/gemini/commands/`, `targets/gemini/hooks/`, and `targets/gemini/policies/`

## Stable Default

- Keep portable concepts in `src/mcp/` and `src/skills/`
- Keep Gemini-native authored content in `src/targets/gemini/...`, including contexts
- Keep Gemini-only semantics in `src/targets/gemini/...`
- Treat `plugin-kit-ai validate --strict` as the CI gate for extension packaging correctness

This scaffold targets official Gemini CLI extension packaging. It does not claim runtime parity with Claude or Codex.
