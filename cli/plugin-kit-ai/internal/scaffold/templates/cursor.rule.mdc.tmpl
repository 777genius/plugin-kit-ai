---
description: Project-wide Cursor guidance for {{.ProjectName}}
globs:
alwaysApply: true
---

- Treat `plugin.yaml` plus `targets/cursor/**` as the authored source of truth.
- Regenerate `.cursor/` artifacts with `plugin-kit-ai render .` instead of editing them manually.
