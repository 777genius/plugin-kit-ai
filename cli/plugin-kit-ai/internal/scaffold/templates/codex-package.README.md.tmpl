# {{.ProjectName}}

Codex package scaffold generated by [plugin-kit-ai](https://github.com/777genius/plugin-kit-ai) `init`.

This lane models the official Codex plugin bundle only.

## Status

- Package lane: `codex-package`
- Status: `production-ready` package contract
- Launcher: not used

## First Run

```bash
plugin-kit-ai generate .
plugin-kit-ai generate --check .
plugin-kit-ai validate . --platform codex-package --strict
```

## Target Files

- `src/plugin.yaml`: canonical shared plugin metadata and target list
- `src/targets/codex-package/package.yaml`: optional Codex-only metadata overrides (`author`, `homepage`, `repository`, `license`, `keywords`)
- `src/targets/codex-package/interface.json`: optional structured Codex `interface` doc generated into `.codex-plugin/plugin.json`; the starter uses the documented `defaultPrompt` array shape
- `src/targets/codex-package/manifest.extra.json`: passthrough only for unsupported future Codex manifest fields; canonical package/interface fields stay managed
- `src/mcp/servers.yaml`: optional portable MCP authored source; `plugin-kit-ai init --extras` pre-seeds a starter and generate projects it into managed `.mcp.json` plus `.codex-plugin/plugin.json#mcpServers`
- `src/targets/codex-package/app.json`: optional Codex app manifest copied to `.app.json`; the starter stays ignored until you replace the empty placeholder with a real app manifest
- root `CLAUDE.md` and `AGENTS.md`: boundary docs that tell humans and agents to edit only `src/`
- `.codex-plugin/plugin.json`: generated managed Codex plugin manifest; `.codex-plugin/` must not contain any other files
- `.app.json` and `.mcp.json`: managed sidecars at the plugin root only when `.codex-plugin/plugin.json` references them

Use this lane for skills, MCP, interface/app metadata, and marketplace-ready package output.
