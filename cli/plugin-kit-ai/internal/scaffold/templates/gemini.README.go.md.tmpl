# {{.ProjectName}}

Generated by `plugin-kit-ai init`.

- Target lane: `gemini`
- Platform family: `extension_package`
- Launcher contract: `optional`
- Runtime claim: `runtime-supported beta extension target`
- Runtime mode: `go`

## Quick Start

```bash
go mod tidy
go test ./...
plugin-kit-ai render .
plugin-kit-ai render --check .
plugin-kit-ai validate . --platform gemini --strict
plugin-kit-ai inspect . --target gemini
plugin-kit-ai capabilities --mode runtime --platform gemini
gemini extensions link .
```

## Files

- `launcher.yaml`: Go entrypoint metadata for the Gemini beta runtime lane
- `cmd/{{.ProjectName}}/main.go`: typed Go hook registrar starter
- `targets/gemini/package.yaml`: Gemini extension metadata
- `targets/gemini/contexts/GEMINI.md`: primary authored Gemini context
- `targets/gemini/hooks/hooks.json`: managed Gemini hook wiring for `SessionStart`, `SessionEnd`, `Notification`, `PreCompress`, `BeforeModel`, `AfterModel`, `BeforeToolSelection`, `BeforeAgent`, `AfterAgent`, `BeforeTool`, and `AfterTool`
- `gemini-extension.json`: rendered native extension manifest

## Notes

- This lane is `public-beta`, not production-ready.
- For the generated Go starter, `gemini.*Continue()` helpers mean a true no-op Gemini hook response. Use `gemini.BeforeToolAllow()` or `gemini.AfterToolAllow()` only when you explicitly want `"decision":"allow"` in the emitted JSON.
- Gemini treats `SessionStart` and `SessionEnd` as advisory hooks, so `continue`, `decision`, `reason`, and `stopReason` are ignored there. Prefer `SessionStartAddContext(...)` and `systemMessage` for lifecycle behavior.
- Gemini treats `Notification` and `PreCompress` as advisory system hooks. They are useful for observability and messaging, but they cannot block the CLI path.
- Use `gemini.SessionStartMessage(...)`, `gemini.SessionEndMessage(...)`, `gemini.NotificationMessage(...)`, and `gemini.PreCompressMessage(...)` when you want a typed helper for advisory `systemMessage` output.
- Use `gemini.BeforeModelOverrideRequestValue(...)` to rewrite `llm_request`, `gemini.BeforeModelSyntheticResponseValue(...)` to short-circuit a model call with a synthetic response, and `gemini.AfterModelReplaceResponseValue(...)` to rewrite returned `llm_response` objects from normal Go maps or structs.
- Use `gemini.BeforeToolSelectionConfig(...)` when you want to steer official Gemini tool selection with `toolConfig.mode` and optional `allowedFunctionNames`. Use `gemini.BeforeToolSelectionDisableAll()` when you intentionally want `mode: "NONE"`.
- Use `gemini.BeforeToolSelectionAllowOnly(...)` when you only want an allowlist, `gemini.BeforeToolSelectionForceAny(...)` when you want Gemini to call at least one tool, and `gemini.BeforeToolSelectionForceAuto(...)` when you want explicit `AUTO` mode with an optional allowlist.
- Use `gemini.BeforeAgentAddContext(...)` for turn-local prompt context, `gemini.BeforeAgentDeny(...)` to discard a prompt, `gemini.AfterAgentDeny(...)` to force a retry, and `gemini.AfterAgentClearContext()` when you intentionally want Gemini to clear prior conversation memory.
- Use `gemini.BeforeAgentStop(...)` when you want to stop the turn but keep the prompt in history, `gemini.AfterAgentStop(...)` when you want to stop the session without retrying, and `gemini.AfterModelStop(...)`, `gemini.BeforeToolStop(...)`, or `gemini.AfterToolStop(...)` when you intentionally want `continue:false` loop-stop behavior.
- Use `gemini.BeforeToolRewriteInputValue(...)` when you want to rewrite `tool_input` from a normal Go map or struct.
- Use `gemini.AfterToolAddContext(...)` to append extra text to the tool result, or `gemini.AfterToolTailCallValue(...)` to request an immediate follow-up tool call with typed Go args.
- `plugin-kit-ai test` and `plugin-kit-ai dev` stay focused on the stable Claude/Codex runtime fixture lanes. For Gemini beta, use `go test ./...`, `plugin-kit-ai render --check .`, `plugin-kit-ai validate . --platform gemini --strict`, `plugin-kit-ai inspect . --target gemini`, `plugin-kit-ai capabilities --mode runtime --platform gemini`, then exercise the extension through `gemini extensions link .` and a real Gemini CLI session.
- Use `make test-gemini-runtime-smoke` for the deterministic repo-local Gemini beta contract smoke before you bother with a real CLI session.
- Use `make test-gemini-runtime-live` when you need the dedicated opt-in real CLI runtime smoke for this lane.
- Keep Gemini-specific authored assets inside `targets/gemini/...`.
- Keep stdout reserved for Gemini hook JSON responses and write diagnostics to stderr only.
